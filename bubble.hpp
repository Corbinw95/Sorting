#include <vector>
#include <iostream>
class bubble{
private:
vector<int> toSort;
public:
  bubble(vector<int> unsorted):unsorted(toSort){  }
  int sort(vector<int> toSort);
  void print(cout out){
    
  }
};
